<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Tests prüfen die reine Pfad-Auflösung ResolveDefaultPath (internal). -->
    <InternalsVisibleTo Include="LinFan.Core.Tests" />
  </ItemGroup>

</Project>
